# Copiar a application-local.yml (gitignored, junto a este archivo) y
# completar la password de tu usuario local de Postgres.
spring:
  datasource:
    username: postgres
    password: tu_password
    # url: jdbc:postgresql://localhost:5432/TIP

# Opcional: pisar el secreto de JWT de application.yml (que es un valor de
# desarrollo commiteado a propósito, ver el comentario ahí) con uno propio.
# fleetguard:
#   jwt:
#     secret: tu_secreto_propio_de_al_menos_32_bytes
